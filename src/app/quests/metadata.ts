import { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Quests | QuestFlow',
  description: 'Manage your quests, create new tasks, and generate AI-powered quests to boost your productivity.',
};

export { default } from './page';
