import { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Dashboard | QuestFlow',
  description: 'Your productivity command center. View stats, daily quests, and quick actions.',
};
